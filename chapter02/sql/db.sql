CREATE TABLE `region` (
                          `region_id` varchar(100) NOT NULL,
                          `region_name` varchar(200) DEFAULT NULL,
                          PRIMARY KEY (`region_id`)
);
